/**
 * The documentation source, synchronously: the typed tables, describe(),
 * and the phase hints. `webmcp-profiler/docs` is for build-time
 * generators and for sites that embed the manifest in their own; the
 * profiler itself loads this module on demand from help() and describe().
 */
export * from "./core/docs"
