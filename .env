# The one authoritative place the deployed origin is written down.
# Used only for the absolute og:url / og:image tags in index.html
# (crawlers need absolute URLs; everything else in the app is
# origin-independent). Change this when the domain moves.
VITE_SITE_URL=https://tryunfolded.com
